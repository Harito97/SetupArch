# Remember you shouldn't run this script 
# Because script will run all as the same time
# No matter the previous script done
# The next command will run 

# So read this script just to know step by step 
# about the way i install all of this

# install needed package fisrt
./Guide/script/install_first.sh
sleep 10m

# install Hyprland GUI - the Desktop environment that i use
./Guide/script/install_DE.sh 
# this will install AUR helper - yay and paru
sleep 20m


# install app that we want
./Guide/script/pacman_installer.sh
sleep 5m

./Guide/script/yay_installer.sh 

